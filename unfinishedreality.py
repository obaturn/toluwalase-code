def find_the_maximum_number(numbers):
	highest_num = numbers[0]
	for elemnt in numbers:
	if highest_num > element
		highest_num= element
	return highest_num
def find_the_minimum_number(numbers):
	smallest_num =  numbers[0]
	for element in numbers:
	if smallest_num < element
		smallest_num = element
	return smallest_num
def diffrencies(number)
checker = highest_num(numbers) - smallest_num(numbers):
return checker

	